{
  "name": "tr3sc3rb3r0-monorepo",
  "private": true,
  "version": "1.0.0",
  "description": "Tr3sC3rb3r0 — Chat IA · CRM · Software a la medida. Monorepo: apps/web (público) + apps/api (backend) + packages/shared.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": { "node": ">=20" },
  "scripts": {
    "dev": "npm run dev --workspace=@tr3sc3rb3r0/web",
    "start": "npm run start --workspace=@tr3sc3rb3r0/web",
    "build": "echo 'Por ahora apps/web no requiere build (estático). Cuando exista apps/api, agregar aquí su build.'",
    "optimize:svg": "npm run optimize:svg --workspace=@tr3sc3rb3r0/web",
    "optimize:raster": "npm run optimize:raster --workspace=@tr3sc3rb3r0/web"
  }
}
